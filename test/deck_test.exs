defmodule Poker.DeckTest do
  use ExUnit.Case, async: true

  alias Poker.Deck

  # test "shuffle/1 a deck of cards" do
  #   deck = Deck.new()
  #   assert Deck.shuffle(deck, 1) != Deck.shuffle(deck, 2)
  # end

  # test "deal hands of cards to a number of players" do
  #   deck = Deck.new()
  #   [h1, h2] = Deck.deal_to(deck, 2)
  #   assert length(h1) == 5
  #   assert length(h2) == 5
  # end
end
